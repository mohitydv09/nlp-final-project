import time
from llm import LLM

## Will need LLM to infer
## Will need access to the deque to get the data.

def make_prompt(data : deque): 
    ## Make the prompt
    pass

def nav_mode():

    while True:
        # get the data
        # make the prompt

        # Sent the prompt to the LLM to get the response

        # if the response is "exit" ## Make the LLM return "exit" if the user says "exit"
        # break

        #else
        ## process the return 
        
        ## Sleep for 10 secs
        # time.sleep(10)
        pass